@model List<CadeteriaMVC.Entidades.Cliente>

<table class="table">
    <tr>
        <th>Nombre</th>
        <th>Direccion</th>
        <th>Telefono</th>
        <th>Modificar</th>
        <th>Eliminar</th>
    </tr>

    @foreach (CadeteriaMVC.Entidades.Cliente C in Model)
    {
        <tr>
            <td>@C.Nombre</td>
            <td>@C.Direccion</td>
            <td>@C.Telefono</td>
            <td>
                <form asp-action="UpdateCliente" method="post" class="form-group">
                    <input type="hidden" name="id" value="@C.Id" />
                    <button type="submit" class="btn btn-outline-info waves-effect">Modificar</button>
                </form>
            </td>
            <td>@Html.ActionLink("Eliminar", "BajaCliente", new { id = C.Id })</td>
        </tr>
    }
</table>